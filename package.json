{
	"private": true,
	"scripts": {
		"dev": "npm-run-all marp:build slidev:build next:dev",
		"next:dev": "cd next && npm i && SLIDES_DIR=\"$(git rev-parse --show-toplevel)/.out\" npm run dev",
		"slidev:build": "npm --prefix slidev run build",
		"marp:build": "npm --prefix marp run build"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
